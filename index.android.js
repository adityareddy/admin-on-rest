import React, { Component } from 'react';
import {
  AppRegistry,
  StyleSheet,
  Text,
  View
} from 'react-native';

import {Admin} from './src';

export default class adminonrest extends Component {
  render() {
    return (
      <View>
      </View>
    );
  }
}

AppRegistry.registerComponent('adminonrest', () => adminonrest);
